name: flutter_desktop_drag_drop
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.7 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cross_file:
  desktop_drop:
  mime:

  window_manager:
  dio: ^4.0.0
  blurry_modal_progress_hud: ^1.1.1
  flutter_spinkit: ^5.2.0
  font_awesome_flutter: ^10.4.0
  toasta: ^1.0.4
  simple_animations: ^1.3.3
  flutter_dropdown_alert: ^1.0.7
  


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - images/

  fonts:
    - family: Kanit
      fonts:
        - asset: fonts/Kanit-Regular.ttf
